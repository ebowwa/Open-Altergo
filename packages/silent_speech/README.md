# Silent speech service

This package exposes Open-Altergo's interface-neutral `RuntimeConfig` and
`SilentSpeechService`. It intentionally has no Gradio dependency.

From the Open-Altergo repository root:

```bash
pip install -e cloud/engine -e packages/silent_speech
```
